import React, { Component } from 'react'

import {MenuBar} from '../_styles/structures/Menu-Bar'

class AppMenuBar extends Component {
  render () {
    return (
      <MenuBar>
        <h1>MENU!</h1>
      </MenuBar>
    )
  }
}

export default AppMenuBar